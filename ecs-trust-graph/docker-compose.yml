services:
  trust-manager:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: >
      sh -c "pip install -r requirements.txt &&
             python -m ecs_trust"
    ports:
      - "8080:8080"
